<h2>Todas as Tarefas:</h2>
{{#if tasks.length}}
  <ul>
    {{#each tasks}}
      <li>
        <div>
          <b>{{this.title}}</b> - {{this.description}}
        </div>

        <div>
          <!-- Status da tarefa -->
          {{#if this.done}}
            <span class="done">✅ Concluída</span>
          {{else}}
            <span class="not-done">🕗 Pendente</span>
          {{/if}}

          <!-- Botão de editar -->
          <a href="/tasks/edit/{{this.id}}" class="edit-btn">Editar</a>

          <!-- Botão de deletar -->
          <form style="display:inline;" method="post" action="/tasks/delete">
            <input type="hidden" name="id" value="{{this.id}}"/>
            <button type="submit">Excluir</button>
          </form>
        </div>
      </li>
    {{/each}}
  </ul>
{{else}}
  <p>🎉 Nenhuma tarefa cadastrada. Que tal criar uma nova?</p>
{{/if}}
